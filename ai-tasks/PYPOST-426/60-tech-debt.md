# PYPOST-426: Technical Debt

## Resolved

- **TD-2 (PYPOST-404)**: `MainWindow.apply_settings` now uses `settings: AppSettings`.

## Remaining (non-blockers)

- **Widget-level fixed `font-size` in QSS** (from PYPOST-106): optional UX audit for very large
  user font sizes — unchanged by this task.
- **`main.py` ConfigManager ordering** ([PYPOST-427](https://pypost.atlassian.net/browse/PYPOST-427)):
  unrelated follow-up.

## Verdict

**SAFE TO CLOSE** — no blockers.
