# PYPOST-898 Requirements

Optional collection-tree open for Seed POST then Send under shared stub.

## Acceptance

- [x] `test_seed_post_open_from_collection_tree_then_send` on seeded session
- [x] Uses `click_tree_row_by_text` + `ui_click(..., in_current_tab=True)`
- [x] Documented in `doc/dev/agent_e2e_http.md`
