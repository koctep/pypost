# PYPOST-990 Developer Documentation

Updated:

- `doc/dev/agent_ui_actions_mcp.md` with optional HTTP invocation, endpoint,
  Qt dispatch, lifecycle, trust boundary, configuration, troubleshooting, and
  Make-driven tests.
- `doc/dev/logging.md` with the five HTTP lifecycle event definitions.
- `Makefile` help text so the existing target points maintainers to the module's
  explicit `--http` option.

The stdio and attach procedures remain documented as the default and live-UI
paths respectively.
