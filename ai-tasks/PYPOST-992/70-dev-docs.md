# PYPOST-992 Developer Documentation

The agent-UI sidecar guide now includes the Make-driven test commands for the
optional HTTP route and the stdio click/fill `call_tool` smoke. No separate
runtime documentation change was needed because the production sidecar
contract is unchanged by this test-only issue.
