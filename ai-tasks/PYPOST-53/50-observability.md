# PYPOST-53: Observability

## Logging

- On successful duplicate: `logger.info` in `EnvironmentDialog` with
  `environment_copied source_name=… new_name=…` (`pypost/ui/dialogs/env_dialog.py`).

## Metrics

- None added for this UI-only change.
