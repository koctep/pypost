# PYPOST-858: Dev Docs

## Created / Updated

- [x] `doc/dev/agent_e2e.md` — marker, shared fixtures, make dual selection
  (Step 3; verified Overview / Architecture / Usage / Configuration /
  Troubleshooting)
- [x] `doc/dev/agent_e2e_env.md` — Implementation status: Session + marker
  row marked Delivered
- [x] `doc/dev/agent_e2e_seed.md` — preferred path
  `seeded_agent_e2e_session`; direct writer / `seeded_agent_dirs` kept for
  multi-session
- [x] `doc/dev/logging.md` — catalog `agent_e2e_fixture_ready`; Related
  link to `agent_e2e.md`; compose note with lifecycle / seed events
- [x] `doc/dev/gui_testing.md` — prefer shared fixtures + `agent_e2e` mark
- [x] `doc/dev/testing.md` — already documents
  `make test-agent-e2e` marker / `PYTEST_ARGS` (verified)
- [x] `doc/dev/README.md` — Testing index already lists umbrella / env /
  seed (verified; no duplicate 858 page needed)

## Notes

Step 3 authored the umbrella packaging sections; Step 7 completes the
deferred logging catalog from Step 5 and aligns seed / GUI docs with the
shared fixture path. Sibling HTTP / artifacts / env-pack CI remain
PYPOST-859–861.

## Worklog

```
tokens_used: 32000
role: execution
step: 7
step_name: steps-3-review-through-7
```
