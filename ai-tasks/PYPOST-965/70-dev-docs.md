# PYPOST-965: Dev Docs

## Updated

- `doc/dev/testing.md` — § Slow smoke isolated workspace seed: name
  `_materialize_slow_smoke_workspace` as the single assembly entry point used by the fixture
  and seed-contract tests.

## Unchanged

- Minimum `pypost/` tree policy (PYPOST-963)
- Parser / packaging field table (PYPOST-964)
