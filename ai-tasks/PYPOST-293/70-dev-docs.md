# PYPOST-293: Dev Docs Update

## Changes

Updated `doc/dev/request_actions.md`:

- Documented plus placeholder tab pattern instead of `TabBarWithAddButton` / manual positioning.
- Updated troubleshooting for `+` button visibility.
- Noted `PLUS_TAB_MARKER` and `_request_tab_count()` for developers counting tabs.

## Validation

- [x] Docs match implemented code in `TabsPresenter`
- [x] New-tab flow (`Ctrl+N` and `+` click) unchanged from user perspective
