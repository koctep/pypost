# PYPOST-1154: Observability

## Assessment

No new logging required. The orchestrator already logs worker count at run start; changing
the default does not alter observability surfaces.

## Result

N/A — document default policy in dev docs only (`doc/dev/parallel_test_runner.md`).
