# PYPOST-156: Dev Docs

## Updated

- `doc/dev/mcp_integration.md` — added `mcp_legacy_sse.py` section under legacy SSE routing.

## Notes

Document that `SSEEndpoint`, `MessagesEndpoint`, and `build_legacy_sse_app` are the shared
implementation for both MCP servers.
