# PYPOST-779: Code Cleanup

## Static analysis

- `make lint` — no new flake8 issues in changed Python files (`tests/test_makefile.py`).

## Formatting

- All edited files use LF endings and trailing-newline convention.
- `requirements.txt` header documents the exact `uv pip compile` command used.

## Makefile

- `lock` and `check-lock` added with `##` help annotations.
- `LOCK_PYTHON_VERSION` and `UV` overridable variables for portability.

## Tests

- New `TestLockFiles` class validates `requirements.in` presence and autogenerated header.
- `test_lock_target_has_no_venv_prerequisites` ensures lock regeneration needs no venv.

## No changes required

- Application code under `pypost/` untouched.
- Dev dependency duplication (Makefile vs CI) deferred to PYPOST-780.
