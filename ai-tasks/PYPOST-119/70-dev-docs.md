# PYPOST-119: Dev Docs

## Updated

- `doc/dev/ui_mixins.md` — added **Scan-position cache (PYPOST-119)** section documenting mixin
  `(scan_text, scan_index)` cache alongside line-scoped scan and table cell cache.

## No other doc changes

Variable propagation and tooltip styling docs unchanged.
