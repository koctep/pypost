# PYPOST-1022: Architecture Design

## Diagram Schematics Architecture

1. **Window Layout Schematic (`interface.md`)**:
   - Outlines the five primary regions of the PyPost desktop client (Menu, Environment bar, Sidebar tree, Request editor tabs, and Response viewer).
   - Formatted in plain-text fenced code block (`text`) to ensure zero external binary dependencies and robust display across web, IDEs, and terminals.

2. **First-Run Request Flow (`getting-started.md`)**:
   - Illustrates the 4-step sequence from environment selection -> composition -> execution -> collection save.
