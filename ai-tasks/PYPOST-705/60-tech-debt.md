# PYPOST-705: Technical Debt

- **Optional bearer token auth** for non-loopback MCP/metrics binds — deferred; document as
  future enhancement in `mcp_trust_model.md`.

No Jira follow-up created (explicitly out of scope for this ticket).
