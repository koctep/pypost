# PYPOST-567: Observability

Analysis-only task. No new metrics or log statements in application code.

The inventory documents existing log noise visible because `pytest.ini` sets
`log_cli = true` and `log_cli_level = WARNING`.
