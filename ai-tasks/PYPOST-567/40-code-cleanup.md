# PYPOST-567: Code Cleanup

No application code changed. Added `scripts/parse_test_log_inventory.py` only.

- Script follows project Python style (UTF-8, type hints, argparse CLI).
- No trailing whitespace; single final newline.
- `tests.txt` is evidence input — not committed (gitignored if added later).
