# PYPOST-734: Observability

N/A — tooling-only task. No runtime logging or metrics changes.

The baseline gate itself is the regression signal: `make typecheck` fails when new type errors
appear in scoped modules.
