# PYPOST-704: Architecture

`is_localhost_bind_host()` in bind validation module; `MetricsServer.start_server` warns when
bind is not loopback. Default change in `AppSettings` only affects new installs.
