# PYPOST-141: Code cleanup

## Changes

- New modules follow existing dataclass + dialog patterns (`mcp_tools_overview`).
- No dead code introduced; activity recording is optional via injected `McpActivityLog`.
- Flake8: verified on touched files.

## Worklog

role: execution, step: 4, step_name: Code cleanup, tokens_used: 800
