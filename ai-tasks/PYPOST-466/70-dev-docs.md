# PYPOST-466: Dev Docs

## Updated Documentation

- Modified `doc/dev/environments_dialog.md` to reflect the new `Move Up` and `Move Down` context menu actions in the variables table.
- Documented how the Python ordered dictionary acts as the single source of truth for UI reordering and serialization.
