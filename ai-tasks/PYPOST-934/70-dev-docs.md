# PYPOST-934 Dev Docs

- `doc/dev/agent_dialog_settle.md` — overview (two tests), architecture table,
  companion usage (`settle_error`, impossible predicate, diag asserts),
  configuration (`FORCED_SETTLE_TIMEOUT_S`, `forced_dialog_settle_timeout`),
  troubleshooting row for companion assertion failures
