# PYPOST-538: Technical Debt Analysis

## Resolved

- **Duplicate context-menu test helpers** — consolidated into `tests/helpers/collections_tree.py`.
- **Split harness modules** — `collection_tree_actions_test_support.py` merged into helpers.

## Remaining (non-blocker)

- **`test_collection_tree_delete_metrics.py` still uses presenter** — optional follow-up to
  adopt isolated harness (noted in PYPOST-537).

## Follow-up Tasks

None required for close.
