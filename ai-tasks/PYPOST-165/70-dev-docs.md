# PYPOST-165: Dev Docs

## Updates

- [x] `doc/dev/environments_dialog.md` — document invalid variable name validation and error
  dialog in Variable Editing section.

## Rationale

Variable validation rules live in `doc/dev/variable_validation.md`; environments dialog doc
now states how the table enforces them and surfaces errors to the user.
