# PYPOST-736: Dev Docs

No doc/dev update needed — `doc/dev/setup.md` already documents `make lint` and
flake8 for local use (referenced from PYPOST-729's dev-docs note). The CI workflow
file itself is self-documenting via step names.
