# PYPOST-200: Per-request timeout configuration (deferred)

## Goals

Track follow-up from [PYPOST-26](https://pypost.atlassian.net/browse/PYPOST-26): user-configurable
timeout per request in `RequestData` and UI.

## Definition of Done

Not in sprint 492 debt-closure scope. Remains open follow-up after PYPOST-194 constant extraction.

## Task Description

Sprint 492 documents deferral. Implementation requires model, UI, and `HTTPClient` wiring.
