# PYPOST-960: Code Cleanup

- Removed duplicate local tuples from `lifecycle.py` and
  `fixtures/agent_e2e_failure.py`.
- New leaf module `e2e_dump_errors.py` has no formatting issues.
- `make analyze` clean on touched files.
