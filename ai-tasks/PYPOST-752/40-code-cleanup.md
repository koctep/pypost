# PYPOST-752: Code Cleanup

No application code changes required — `pypost/` already has zero `print()` calls after
PYPOST-742.

Config-only diff: `.flake8`, `Makefile`, `.github/workflows/test.yml`, dev docs.
