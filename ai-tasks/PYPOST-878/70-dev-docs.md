# PYPOST-878: Dev Docs

## Updates

- `doc/dev/gui_testing.md` — document that `gateway_timeout_detail` includes
  optional `worker_operation=` for env workers; example failure text; note
  collection load-only omission (PYPOST-878).
- `doc/dev/environment_storage_async.md` — mention optional
  `worker_operation=` alongside busy/pending/worker_running on gateway waits.

## Artifact

This file records Step 8 completion for the task folder.
