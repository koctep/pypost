# PYPOST-676 Roadmap

**Task:** Call sites that only need counters still receive full `MetricsManager` facade (deferred)
**Language:** Python 3.10+

## Steps

- [x] STEP 1–7 (closure — deferred metrics injection refinement)
